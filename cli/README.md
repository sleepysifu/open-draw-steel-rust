## Running

```bash
cargo run --package crystal-heart-cli
```

## Controls

- Press `n` to create a new battle
- Press a letter to take a turn for that entity
- Press `r` to complete the round
- Press `q` to quit