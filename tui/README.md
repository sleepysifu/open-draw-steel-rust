## Running the TUI

open a terminal within the repository and run:
```bash
cargo run --package odsr-tui
```

## Controls

- Press `n` to create a new combat
- Press a letter to take a turn for that entity
- Press `r` to complete the round
- Press `q` to quit